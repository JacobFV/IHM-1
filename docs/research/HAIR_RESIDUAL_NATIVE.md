# Isolated hair residual native initialization

`scripts/materialize_hair_residual_native.py` writes a fresh isolated source input directory. It revalidates the hash-bound 94-guide partition and replaces the source XML body mass, center of mass and full inertia tensor with the actual frozen residual values. This occurs before native model loading, system initialization and muscle reference initialization. The candidate uses the existing reduced 22-body topology; scalp remains structurally assigned to torso.

The native target is exactly the residual XML total, **77.61218724193209 kg**, so the existing native scaling stage must evaluate to exactly 1. Adding the independent hair mass, 0.00001565806789575483 kg, recovers the original 77.6122029 kg. No canonical proxy is debited, no intake-owned mass transfer is performed, and no unrepresented population mass is removed.

`scripts/verify_hair_residual_native.py` without arguments runs a bounded source fixture. It constructs the isolated XML, validates all 22 masses/COMs/full tensors, initializes the actual beam nodal masses with nonzero parent rigid velocities, and checks linear momentum, angular momentum and kinetic energy closure. It rejects hidden scaling and perturbed off-diagonal inertia. The light fixture passed in 0.22 s with 75,432 KiB RSS.

With a coordinated `--run-native` slot, the fixture reuses a hash-verified existing native binary, opens the isolated model in zero gravity, receives one initial snapshot, and closes. It requests no integrated interval. A 1 GiB address-space cap, 30 s CPU cap, 45 s wall timeout, one CPU and reduced scheduling priority bound the run. Small nonzero pelvis translation and rotation defaults are explicitly engineered initialization values, not observations. All guide material node velocities are evaluated from the loaded parent origin velocity plus angular velocity crossed with the exact transformed station displacement. Initially straight rods carry no declared deformation energy.

The native acceptance compares the actual loaded Simbody mass properties for all 22 bodies against the residual receipt, cross-checks native kinetic energy, confirms that the intake mass port is disabled and unused, and checks residual body plus guide momentum/angular momentum/energy against the original actual bodies at the same kinematic state. Runtime library and binary hashes are retained with the receipt.

Neither the materializer nor the acceptance promotes a model to live use. A successful initialization still leaves the single coupled hair/body interval, checkpoint/replay, full contact coverage and authoritative viewer synchronization unverified. The two inner-shell guide roots remain unchanged and excluded, as recorded in `HAIR_NATIVE_PARTITION.md`.
