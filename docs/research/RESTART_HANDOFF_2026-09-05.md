# Restart handoff: increased agent capacity

User requested 16 concurrent spawned-agent threads; user config updated and
`codex doctor --json` reports config.load=ok. Current session still exposes four
total slots. Resume this conversation after restarting/reopening the host and
check actual exposed capacity. No guarantee that the host adopts the CLI setting.

Read WHOLE_BODY_REMAINING_CONCERNS.md and PARALLEL_RND_WORKFLOW.md first.
Root runtime/API work remains uncommitted in the shared workspace; preserve it.
Do not overwrite the user's four untracked embodied plans/specs or test-results.

## Latest completed worker handoffs

- 756078c mechanics and 89653d2 garment foundation. Actual 92-muscle native
  acceptance passed at data/derived/articulated-acceptance-noo34jds: 1.32 seconds
  wall, 99.2 MiB native peak, 2408 entities, force response, metabolic response,
  exact checkpoint replay. No settled support or native full-garment acceptance.
- 64103cc Arm26 migration repair. Use
  data/derived/mechanics/whole_body_arm26_v2/registration.json. V1 is invalid
  for this native loader; retain its failed receipt.
- d740154 EmbodiedRespiration helper: composed articulated breathing geometry
  and work-conjugate load projection. Not yet wired into root runtime.
- 441f635 unified UI source: 11 small checks pass, production build pending
  actual integrated runtime acceptance. Running server remains old code.
- 7ac07d3 EMBODIED_ACCEPTANCE_REVIEW.md reproduces actor cleanup falsely
  reporting closed after owner cleanup raises. Root must fix before acceptance.
- b6c8eb4 GI_FAILURE_NEXT_EXPERIMENT.md specifies cheap discriminating probes.

All three workers completed; mechanics reports no surviving native job and
released the heavy-job slot. Recheck owned processes on resume.

## Root critical path

Fix actor cleanup/resource ownership and checkpoint release boundary. Wire v2
92-muscle registration into factory. Replace mechanical-work proxy with native
muscle metabolic cost using an explicit reference and no duplicate basal demand.
Wire EmbodiedRespiration geometry/load including supported contact wrenches.
Review native horizon handling and state selection. Run the smallest integrated
native acceptance, then one UI production build and controlled viewer check.
Do not rerun already completed native component acceptance without new cause.

## Independent candidate assignments with increased capacity

Each needs exact file ownership and bounded acceptance before dispatch:

1. Root shared runtime integration and state/energy ownership.
2. Actor/API cleanup, sequence and recovery defects.
3. Anatomical registration and joint-continuity correction.
4. Supine support equilibrium and contact diagnostics.
5. Whole-garment native coupling and containment.
6. Peripheral sensory/motor coverage and named effector mapping.
7. Organ-specific fine vascular data/conditional synthesis contracts.
8. Regional lymph topology and local compression mapping.
9. Layered integument and bioelectric evidence/parameter audit.
10. GI shared-donor transport and carbon/counterion integrity probes.
11. Live intake scheduling with explicit native command ownership.
12. Hair solver/contact performance work.
13. UI live monitor/stimulus controls and acceptance.
14. Temporal/Laplace materialization correctness audit.
15. Source uncertainty and coefficient coverage audit.
16. Independent cross-system conservation review.

These are available lanes, not permission for 16 simultaneous native runs. Root
must resolve interface overlaps before assigning edits. Parallel source work;
one heavy local job at a time with memory bounds. Protect concurrent IBM-1.
