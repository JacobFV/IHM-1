# Isolated shared hair/body interval candidate

`ihm/assembly/hair_coupled_candidate.py` combines all 94 eligible independent guides and their 278 exact retained source nodes into one `HairFeedback` owner. Source face/node identities and fixed inferred body owners are preserved. The frozen canonical-to-native registration maps moving roots, clamp directions, body surface points and reaction wrenches. Every hair material node starts with its parent rigid station velocity. The actual residual native model is initialized before constructing this owner.

The native fixture uses one process and actual native checkpoint/restore/drop commands. Prediction and fixed-point iterations are repeatedly restored to the same interval start. Only one converged interval is retained; scalp and body hair do not separately advance the native body. No garment or other subsystem is stepped in this isolated fixture.

`scripts/verify_hair_coupled_native.py` without flags passed its source-only check: one owner, all 94 guides, 278 exact nodes, matched moving clamps, nonzero velocities, exact hair checkpoint restoration. The gated `--run-native` fixture requests two 0.1 ms physical intervals, a third interval replayed from a checkpoint, and a forced nonconvergence attempt which must restore both physical clocks, body state, hair state and native work/heat ledgers. The native child has a 1 GiB address-space cap and 30 s CPU cap; a 45 s alarm bounds the complete acceptance. No compilation or shared runtime change is required.

Native acceptance checks nonzero body reaction impulse, hair/body internal impulse balance, total linear/angular momentum, exact agreement between native external work increment and the hair interface quadrature, matched physical clocks, replay and rollback. Reports retain native kinetic/potential energy change, signed active-fiber work, muscle heat, and hair interface/numerical/contact receipts. Active native muscles and passive tissue remain present: this does not establish a closed full-native mechanical energy certificate.

Contact coverage is only the 94 sparse follicle faces. It remains a face-interior, centerline, small-deflection beam experiment with zero radial clearance; edges, continuous collision detection, self-contact, complete body coverage, and garment contact are absent. Current scalp-to-torso ownership is a reduced-topology structural approximation. No live model or viewer is enabled.
