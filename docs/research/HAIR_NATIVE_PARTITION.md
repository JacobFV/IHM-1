# Hair source ownership and candidate native partition

This source-only preparation does not install or enable native hair. The current reduced model has no separate native head: scalp guides map to **torso**, a structural approximation, not anatomical scalp ownership. Any revised cervical topology invalidates this partition.

`data/research/hair_native_partition.json` binds immutable captured inputs to their SHA256 receipts: the original exact sample-ID/face join, frozen scaled 22-body inertia and reference transforms, neutral source contact registration, and engineered skin component classification. Source nodes and roots never move. Ownership uses the existing nearest named bone AABB prior. Connected retained follicle faces require one rigid clamp owner; a mixed component uses minimum summed squared envelope distance, retaining the raw nearest owner and override evidence. This is engineered synthesis, not calibrated anatomy.

The candidate includes 94 exact exterior follicle faces and 278 source nodes in 90 retained connected components. One component overrides one node (7389, radius_r to ulna_r); three nodes have nearest-owner margins within 1 mm. Sparse retained faces do not establish whole-skin continuity or complete collision coverage.

The source has nested opposing shells. The selected largest exterior component is an engineering surface proxy with open boundaries, not a closed-volume proof. Exact original attachments show:

| Population | Exterior | Inner | Seam |
|---|---:|---:|---:|
| Scalp, original 92,934 | 92,934 | 0 | 0 |
| Body, original 201,528 | 189,701 | 11,825 | 2 |
| Retained scalp guides | 64 | 0 | 0 |
| Retained body guides | 30 | 2 | 0 |

Body guide sample 185247 remains on source face 174273; sample 145098 remains on face 146232. Both are explicitly physically ineligible for this candidate, never reattached. Original population and guide assets remain unchanged. The excluded original body population has 9.070628311 mg of density/radius/length prior mass; this is evidence, not a calibrated compartment mass.

The 94 eligible independent guides represent 15.6580678958 mg across nine reduced native owners. Their actual nodal mass, first moment and full second moment are transformed into each owner's frozen reference frame and subtracted from its actual scaled mass/COM/inertia. Canonical hair proxies are descriptive and are **not** separately integrated native mass; no proxy debit or population weight is used. Unmaterialized hair stays within residual lumped bodies. Every residual centered second-moment tensor must remain positive. Recombination preserves 77.6122029 kg with zero mass residual, first-moment residual norm 4.45e-15 kg m, and second-moment residual norm 1.08e-14 kg m².

Validation: `scripts/verify_hair_source_ownership.py` checks exact identity preservation, disclosed ambiguity/coherence overrides, incompatible source rejection, mass/full-moment conservation, invalid residual inertia rejection, artifact hashes, and disabled native state. `scripts/materialize_hair_native_partition.py` ran single-threaded in 0.37 s, peak 127,676 KiB RSS; no native process, browser, build, or full mesh decode.

Remaining activation work: install residual native inertia with exact provenance; initialize guides with matching rigid-body velocities and momentum/energy receipt; use a single shared coupled physical interval; supply adequate moving surface coverage; synchronize authoritative viewer state. This artifact alone does none of those steps.
